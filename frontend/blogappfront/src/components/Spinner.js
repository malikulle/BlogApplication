import React from "react";

const Spinner = () => {
  return (
    <div className="spinner-border text-warning m-auto">
      <span className="sr-only">Loading...</span>
    </div>
  );
};

export default Spinner;
